@model Developer_Skills_Assessment.Models.StudentCourseCreateViewModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>StudentCourseCreateViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SelectedStudentId" class="control-label"></label>
                <select asp-for="SelectedStudentId" asp-items="ViewBag.Students"></select>
                <span asp-validation-for="SelectedStudentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SelectedCourseId" class="control-label"></label>
                <select asp-for="SelectedCourseId" asp-items="ViewBag.Courses"></select>
                <span asp-validation-for="SelectedCourseId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
